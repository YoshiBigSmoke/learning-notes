git status

to know about what we dont send to the repository on github

git add . #for everythyng

git add (file) #for a specific file

git commit -m "modify " #to confirm it 

git push origin (name of the main file) #to send it 

this is the simplest way to make a commit on git
to sent it to github